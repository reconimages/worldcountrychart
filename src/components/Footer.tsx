export default function Footer() {
  return (
    <footer className="border-t border-edge">
      <div className="mx-auto w-full max-w-5xl px-4 py-8 sm:px-6">
        <p className="text-center text-xs leading-relaxed text-mute">
          Chart positions 1–3 (Maddox Chain, Raven Cross, Wade Ashmore) are permanently
          featured and cannot be re-ranked. Remaining positions refresh once per day.
        </p>
        <p className="mt-2 text-center text-[11px] text-mute/70">
          Streaming links open the official artist pages on each platform.
        </p>
      </div>
    </footer>
  );
}